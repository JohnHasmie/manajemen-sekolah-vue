// Retired in Phase 1 (#186). Coach-mark tour plumbing is dropped — the
// shared `AdminCrudScaffold` header is self-explanatory enough that the
// guided tour is no longer worth the maintenance cost. Safe to delete
// from the working tree.
