// Retired in Phase 1 (#186). Chip construction moved to
// `controllers/helpers/classroom_filter_helper.dart` + exposed via
// `AdminClassroomController.buildActiveFilterChips`. Safe to delete
// from the working tree.
