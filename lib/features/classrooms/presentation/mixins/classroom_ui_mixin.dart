// Retired in Phase 1 (#186). Menu items now go through shared
// `AdminDataMenu`; filter chips + filter button are rendered by
// `AdminCrudScaffold` / `ActiveFilterChips`. Safe to delete from the
// working tree.
