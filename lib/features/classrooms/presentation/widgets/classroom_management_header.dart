// Retired in Phase 1 (#186). Bespoke gradient header replaced by the
// shared `AdminCrudScaffold` header (title + subtitle + search +
// filter + active-chips bar). Safe to delete from the working tree.
