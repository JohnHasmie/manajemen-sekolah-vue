// Deprecated. Superseded by the shared `AppBottomSheet` header.
//
// The custom gradient header this mixin used to build has been replaced
// by the canonical drag-handle → gradient-header pattern provided by
// `AppBottomSheet`. This stub stays in place only because the sandbox
// filesystem on this workspace cannot physically remove the file;
// `git rm` on commit will stage the deletion. No code should import
// from this file.
