// Deprecated. Superseded by the shared `BottomSheetFooter`.
//
// The hand-rolled Cancel/Save footer row this mixin used to build has
// been replaced by the canonical `BottomSheetFooter` used across all
// admin edit sheets. This stub stays in place only because the sandbox
// filesystem on this workspace cannot physically remove the file;
// `git rm` on commit will stage the deletion. No code should import
// from this file.
