// Retired in Phase 1 (#192). Active filter chips row is rendered by the
// shared `ActiveFilterChips` widget invoked through `AdminCrudScaffold`
// with typed `ActiveFilter` items built by
// `SubjectFilterHelper.buildActiveFilterChips`. Safe to delete from the
// working tree.
