// Retired in Phase 1 (#192). Bespoke gradient header, search bar, filter
// chips row, and list view are replaced by the shared `AdminCrudScaffold`
// + `AdminDataMenu` + `PaginatedListView` stack called inline from
// `AdminSubjectManagementScreenState.build`. Safe to delete from the
// working tree.
