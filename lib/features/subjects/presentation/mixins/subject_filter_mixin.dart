// Retired in Phase 1 (#192). Filter state (status / classes-status /
// grade-level / class-name) lives inline in `AdminSubjectManagementScreenState`
// with per-chip removal callbacks; chip construction moved to
// `controllers/subject_filter_helper.dart::buildActiveFilterChips`.
// Safe to delete from the working tree.
