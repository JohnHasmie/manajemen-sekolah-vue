// Retired in Phase 1 (#192). Cache/API loading, pagination, and FCM sync
// listener now live inline in `AdminSubjectManagementScreenState` — delegated
// to `AdminSubjectController` + `SubjectDataHelper`. Safe to delete from the
// working tree.
