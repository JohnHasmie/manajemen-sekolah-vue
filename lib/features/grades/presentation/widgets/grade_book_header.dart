// Retired 2026-05-09 — replaced by `BrandPageHeader` directly
// inside `grade_book_screen.dart`.
//
// The legacy bespoke header (gradient bar with back / view-toggle /
// export / close buttons) was specific to the old modal-bottom-sheet
// presentation of the grade book. The grade book is now a real page
// route and uses the shared `BrandPageHeader` like every other
// teacher screen. Keeping the file empty (rather than deleting)
// because the sandbox blocks file deletion; the file can be safely
// removed in a follow-up commit once `dart analyze` reports no
// remaining imports.
