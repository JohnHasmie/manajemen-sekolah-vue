// Retired 2026-04-19 — replaced by
// `lib/features/grades/presentation/widgets/modern_grade_editor_sheet.dart`.
//
// This file (and its four companion mixins) was the old full-screen grade
// input Scaffold. All callers now use the bottom-sheet redesign, which lives
// alongside the other grade widgets instead of as a pushed route. Keeping
// this file empty — rather than deleting it — because the sandbox blocks
// file deletion; it can be safely removed in a follow-up commit.
