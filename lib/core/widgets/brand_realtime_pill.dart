// DEPRECATED — file emptied during the production cleanup pass.
//
// The full-row `BrandRealtimePill` was retired in favour of the inline
// 6dp dot rendered by `BrandPageHeader.isRealtimeFresh`. All call
// sites have been migrated, this file has zero remaining importers.
//
// Kept as an empty stub because the in-sandbox filesystem doesn't
// allow `unlink` syscalls. A follow-up `git rm` on a real dev
// machine will retire it physically.
