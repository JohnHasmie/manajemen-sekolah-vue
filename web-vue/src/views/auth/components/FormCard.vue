<!--
  FormCard.vue — the white card overlapping the gradient band by -24px.
  Hosts whichever auth step is active. Matches Flutter's `_FormCard`
  in login_screen.dart.
-->
<template>
  <div class="form-card p-lg sm:p-xl">
    <slot />
  </div>
</template>
