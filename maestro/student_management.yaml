appId: com.example.manajemensekolah
---
# Test: Student Management — view + search
- runFlow: login.yaml

# Navigate to student management
- tapOn: "Kelola Siswa"
- extendedWaitUntil:
    visible: ".*"
    timeout: 10000

# Verify list loaded
- takeScreenshot: screenshots/student_list

# Go back
- back
